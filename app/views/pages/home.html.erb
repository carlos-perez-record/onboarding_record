<div class="text-center py-20 px-5">
  <h1 class="heading-1 mb-6">Bienvenido a Onboarding Record</h1>
  <p class="text-body-lg mb-12">Sistema de reclutamiento y onboarding de personal</p>

  <% if user_signed_in? %>
    <div class="alert-welcome mb-12">
      <h2 class="heading-3 mb-4">✅ ¡Bienvenido, <%= current_user.email %>!</h2>
      <p class="text-body mb-0">Tu sesión está activa y puedes comenzar a usar el sistema.</p>
      
      <% if current_user.aspirante? %>
        <% curriculum = current_user.curriculum %>
        <% if curriculum&.persisted? %>
          <%= link_to "Ver mi currículum", curriculum_path(curriculum), class: "btn btn-success mt-4", "aria-label": "Ver mi currículum registrado" %>
        <% else %>
          <div class="mt-6 bg-white p-6 rounded-lg text-left">
            <h4 class="heading-4 mb-4">📋 Aún no has registrado tu currículum</h4>
            <p class="text-body mb-4">Para poder aplicar a ofertas laborales, necesitas completar tu información profesional.</p>
            <%= link_to "Crear mi currículum ahora", new_curriculum_path, class: "btn btn-success btn-lg", "aria-label": "Crear mi currículum profesional" %>
          </div>
        <% end %>
      <% elsif current_user.reclutador? %>
        <%= link_to "Ir al panel de reclutamiento", recruiter_aspirants_path, class: "btn btn-success mt-4", "aria-label": "Acceder al panel de reclutamiento" %>
      <% elsif current_user.admin? %>
        <%= link_to "Ir al panel de administración", admin_users_path, class: "btn btn-success mt-4", "aria-label": "Acceder al panel de administración" %>
      <% end %>
    </div>
    
    <div class="mt-12 max-w-4xl mx-auto">
      <h3 class="heading-3 mb-8">Opciones disponibles:</h3>
      
      <% if current_user.aspirante? %>
        <!-- Enlaces a currículum movidos al menú lateral -->
      <% elsif current_user.reclutador? %>
        <div class="card max-w-2xl mx-auto text-center">
          <h4 class="heading-4 mb-2">Gestión de Aspirantes</h4>
          <p class="text-body mb-6">Administra los aspirantes registrados en el sistema.</p>
          <%= link_to "Ir al panel de reclutamiento", recruiter_aspirants_path, class: "btn btn-primary", "aria-label": "Ir al panel de reclutamiento de aspirantes" %>
        </div>
      <% elsif current_user.admin? %>
        <div class="card max-w-2xl mx-auto text-center">
          <h4 class="heading-4 mb-2">Gestión de Usuarios</h4>
          <p class="text-body mb-6">Administra todas las cuentas de usuarios del sistema.</p>
          <%= link_to "Ir al panel de administración", admin_users_path, class: "btn btn-danger", "aria-label": "Ir al panel de administración de usuarios" %>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto mb-12">
      <div class="card border-2 border-green-600">
        <h3 class="heading-4 mb-4">🚀 ¿Eres nuevo?</h3>
        <p class="text-body mb-6">Únete ahora y accede a todas las funcionalidades del sistema.</p>
        <%= link_to "Comienza ahora", new_user_registration_path, class: "btn btn-success btn-lg", "aria-label": "Crear una nueva cuenta de usuario" %>
      </div>

      <div class="card">
        <h3 class="heading-4 mb-4">¿Ya tienes cuenta?</h3>
        <p class="text-body mb-6">Inicia sesión con tu correo electrónico y contraseña.</p>
        <%= link_to "Iniciar sesión", new_user_session_path, class: "btn btn-primary", "aria-label": "Iniciar sesión con tu cuenta existente" %>
      </div>
    </div>

    <div class="mt-12 text-center max-w-2xl mx-auto">
      <h3 class="heading-5 mb-4">¿Olvidaste tu contraseña?</h3>
      <p class="text-body-light mb-6">No te preocupes, puedes recuperarla fácilmente.</p>
      <%= link_to "Recuperar contraseña", new_user_password_path, class: "link", "aria-label": "Recuperar contraseña olvidada" %>
    </div>
  <% end %>
</div>
