<div class="home-hero">
  <h1>Bienvenido a Onboarding Record</h1>
  <p>Sistema de reclutamiento y onboarding de personal</p>

  <% if user_signed_in? %>
    <div class="welcome-box">
      <h2>✅ ¡Bienvenido, <%= current_user.email %>!</h2>
      <p>Tu sesión está activa y puedes comenzar a usar el sistema.</p>
      
      <% if current_user.aspirante? %>
        <% curriculum = current_user.curriculum %>
        <% if curriculum&.persisted? %>
          <%= link_to "Ver mi currículum", curriculum_path(curriculum), class: "btn btn-success btn-action", "aria-label": "Ver mi currículum registrado" %>
        <% else %>
          <div class="no-curriculum-message">
            <h4>📋 Aún no has registrado tu currículum</h4>
            <p>Para poder aplicar a ofertas laborales, necesitas completar tu información profesional.</p>
            <%= link_to "Crear mi currículum ahora", new_curriculum_path, class: "btn btn-success btn-large", "aria-label": "Crear mi currículum profesional" %>
          </div>
        <% end %>
      <% elsif current_user.reclutador? %>
        <%= link_to "Ir al panel de reclutamiento", recruiter_aspirants_path, class: "btn btn-success btn-action", "aria-label": "Acceder al panel de reclutamiento" %>
      <% elsif current_user.admin? %>
        <%= link_to "Ir al panel de administración", admin_users_path, class: "btn btn-success btn-action", "aria-label": "Acceder al panel de administración" %>
      <% end %>
    </div>
    
    <div class="options-section">
      <h3>Opciones disponibles:</h3>
      
      <% if current_user.aspirante? %>
        <!-- Enlaces a currículum movidos al menú lateral -->
      <% elsif current_user.reclutador? %>
        <div class="card card-center">
          <h4>Gestión de Aspirantes</h4>
          <p>Administra los aspirantes registrados en el sistema.</p>
          <%= link_to "Ir al panel de reclutamiento", recruiter_aspirants_path, class: "btn btn-primary", "aria-label": "Ir al panel de reclutamiento de aspirantes" %>
        </div>
      <% elsif current_user.admin? %>
        <div class="card card-center">
          <h4>Gestión de Usuarios</h4>
          <p>Administra todas las cuentas de usuarios del sistema.</p>
          <%= link_to "Ir al panel de administración", admin_users_path, class: "btn btn-danger", "aria-label": "Ir al panel de administración de usuarios" %>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="card-grid">
      <div class="card card-highlight">
        <h3>🚀 ¿Eres nuevo?</h3>
        <p>Únete ahora y accede a todas las funcionalidades del sistema.</p>
        <%= link_to "Comienza ahora", new_user_registration_path, class: "btn btn-success btn-large", "aria-label": "Crear una nueva cuenta de usuario" %>
      </div>

      <div class="card">
        <h3>¿Ya tienes cuenta?</h3>
        <p>Inicia sesión con tu correo electrónico y contraseña.</p>
        <%= link_to "Iniciar sesión", new_user_session_path, class: "btn btn-primary", "aria-label": "Iniciar sesión con tu cuenta existente" %>
      </div>
    </div>

    <div class="password-recovery">
      <h3>¿Olvidaste tu contraseña?</h3>
      <p>No te preocupes, puedes recuperarla fácilmente.</p>
      <%= link_to "Recuperar contraseña", new_user_password_path, class: "btn-link", "aria-label": "Recuperar contraseña olvidada" %>
    </div>
  <% end %>
</div>
