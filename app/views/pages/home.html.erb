<div class="text-center py-12 px-5">
  <% if user_signed_in? %>
    <div class="alert-welcome mb-12">
      <h2 class="heading-3 mb-4">✅ ¡Bienvenido, <%= current_user.email %>!</h2>
      <p class="text-body mb-0">Tu sesión está activa y puedes comenzar a usar el sistema.</p>

      <% if current_user.aspirante? %>
        <% curriculum = current_user.curriculum %>
        <% if curriculum&.persisted? %>
          <%= link_to "Ver mi currículum", curriculum_path(curriculum), class: "btn btn-success py-2 px-4 mt-4", "aria-label": "Ver mi currículum registrado" %>
        <% else %>
          <div class="mt-6 bg-white p-6 rounded-lg text-left">
            <h4 class="heading-4 mb-4">📋 Aún no has registrado tu currículum</h4>
            <p class="text-body mb-4">Para poder aplicar a ofertas laborales, necesitas completar tu información profesional.</p>
            <%= link_to "Crear mi currículum ahora", new_curriculum_path, class: "btn btn-success py-3 px-6", "aria-label": "Crear mi currículum profesional" %>
          </div>
        <% end %>
      <% elsif current_user.reclutador? %>
        <%= link_to "Ir al panel de reclutamiento", recruiter_aspirants_path, class: "btn btn-success py-2 px-4 mt-4", "aria-label": "Acceder al panel de reclutamiento" %>
      <% elsif current_user.admin? %>
        <%= link_to "Ir al panel de administración", admin_users_path, class: "btn btn-success py-2 px-4 mt-4", "aria-label": "Acceder al panel de administración" %>
      <% end %>
    </div>

    <!-- Convocatorias activas para aspirantes (FUERA DEL RECUADRO VERDE) -->
    <% if current_user.aspirante? && @job_postings&.any? %>
      <div class="mb-12 max-w-7xl mx-auto px-4">
        <h3 class="text-xl font-bold text-gray-900 mb-6">📢 Convocatorias Activas</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <% @job_postings.each do |job_posting| %>
            <%= render "job_postings/card", job_posting: job_posting %>
          <% end %>
        </div>
      </div>
    <% end %>

    <div class="mt-12 max-w-4xl mx-auto">
      <h3 class="heading-3 mb-8">Opciones disponibles:</h3>

      <% if current_user.aspirante? %>
        <!-- Enlaces a currículum movidos al menú lateral -->
      <% elsif current_user.reclutador? %>
        <div class="card max-w-2xl mx-auto text-center">
          <h4 class="heading-4 mb-2">Gestión de Aspirantes</h4>
          <p class="text-body mb-6">Administra los aspirantes registrados en el sistema.</p>
          <%= link_to "Ir al panel de reclutamiento", recruiter_aspirants_path, class: "btn btn-primary py-2 px-4", "aria-label": "Ir al panel de reclutamiento de aspirantes" %>
        </div>
      <% elsif current_user.admin? %>
        <div class="card max-w-2xl mx-auto text-center">
          <h4 class="heading-4 mb-2">Gestión de Usuarios</h4>
          <p class="text-body mb-6">Administra todas las cuentas de usuarios del sistema.</p>
          <%= link_to "Ir al panel de administración", admin_users_path, class: "btn btn-danger py-2 px-4", "aria-label": "Ir al panel de administración de usuarios" %>
        </div>
      <% end %>
    </div>
  <% else %>
    <!-- Convocatorias activas para público (SUBIDO) -->
    <% if @job_postings&.any? %>
      <div class="mb-16 max-w-7xl mx-auto px-4">
        <h3 class="text-xl font-bold text-gray-900 mb-6">📢 Convocatorias Activas</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <% @job_postings.each do |job_posting| %>
            <%= render "job_postings/card", job_posting: job_posting %>
          <% end %>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
