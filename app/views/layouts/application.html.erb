<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Registro de Incorporación" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Registro de Incorporación">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <nav class="navbar" aria-label="Navegación principal">
      <div class="max-w-full px-3 sm:px-6">
        <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center py-3 gap-3">
          <h1 class="text-lg sm:text-xl font-bold m-0" style="color: #333;">
            <%= link_to "Registro de Incorporación", root_path, class: "no-underline", style: "color: #333;", "aria-label": "Ir a la página de inicio" %>
          </h1>

          <div class="flex flex-col sm:flex-row items-stretch sm:items-center gap-2 sm:gap-3">
            <% if user_signed_in? %>
              <span class="font-semibold text-sm sm:text-base text-center sm:text-left sm:mr-2" style="color: #333;">Hola, <%= current_user.email %></span>
              <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, form: { style: "display:inline-block; width: 100%;" }, params: {}, class: "btn btn-danger btn-sm sm:btn-md w-full sm:w-auto", "aria-label": "Cerrar sesión de usuario" %>
            <% else %>
              <%= link_to "Crear cuenta", new_user_registration_path, class: "btn btn-primary btn-sm sm:btn-md w-full sm:w-auto text-center", "aria-label": "Crear una nueva cuenta" %>
              <%= link_to "Iniciar sesión", new_user_session_path, class: "btn btn-success btn-sm sm:btn-md w-full sm:w-auto text-center", "aria-label": "Iniciar sesión en el sistema" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <div class="flex">
      <!-- Sidebar Menu (visible only when signed in and on md+ screens) -->
      <% if user_signed_in? %>
        <aside class="sidebar hidden md:block" role="navigation" aria-label="Menú lateral de navegación">
          <nav class="px-4" aria-label="Enlaces de navegación">
            <div class="mb-6">
              <p class="sidebar-title">Menú</p>
              <ul class="list-none p-0 m-0 space-y-2">
                <li>
                  <%= link_to "Inicio", root_path, class: "sidebar-link", "aria-label": "Ir a la página de inicio" %>
                </li>
                <% if current_user.aspirante? %>
                  <li>
                    <% curriculum = current_user.curriculum %>
                    <% if curriculum&.persisted? %>
                      <%= link_to "Mi currículum", curriculum_path(curriculum), class: "sidebar-link", "aria-label": "Ver mi currículum" %>
                    <% else %>
                      <%= link_to "Crear mi currículum", new_curriculum_path, class: "sidebar-link", "aria-label": "Crear mi currículum profesional" %>
                    <% end %>
                  </li>
                <% end %>
              </ul>
            </div>

            <!-- Admin Menu -->
            <% if current_user.admin? %>
              <div class="mb-6 pt-6 border-t border-gray-300">
                <p class="sidebar-title">Administración</p>
                <ul class="list-none p-0 m-0 space-y-2">
                  <li>
                    <%= link_to "Gestión de Usuarios", admin_users_path, class: "sidebar-link-admin", "aria-label": "Administrar usuarios del sistema" %>
                  </li>
                </ul>
              </div>
            <% end %>

            <!-- Recruiter Menu -->
            <% if current_user.reclutador? %>
              <div class="mb-6 pt-6 border-t border-gray-300">
                <p class="sidebar-title">Reclutamiento</p>
                <ul class="list-none p-0 m-0 space-y-2">
                  <li>
                    <%= link_to "Gestión de Aspirantes", recruiter_aspirants_path, class: "sidebar-link-recruiter", "aria-label": "Administrar aspirantes registrados" %>
                  </li>
                </ul>
              </div>
            <% end %>
          </nav>
        </aside>
      <% end %>

      <div class="flex-1 p-3 sm:p-6">
        <% if notice %>
          <div role="status" aria-live="polite" class="bg-green-100 border border-green-300 p-3 sm:p-4 mb-4 sm:mb-6 rounded text-sm sm:text-base">
            <strong class="text-green-800"><%= notice %></strong>
          </div>
        <% end %>

        <% if alert %>
          <div role="alert" aria-live="assertive" class="bg-red-100 border border-red-300 p-3 sm:p-4 mb-4 sm:mb-6 rounded text-sm sm:text-base">
            <strong class="text-red-800"><%= alert %></strong>
          </div>
        <% end %>

        <main id="main-content" role="main">
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>
