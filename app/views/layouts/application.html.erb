<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Registro de Incorporación" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Registro de Incorporación">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar">
      <div class="navbar-container">
        <h1 class="navbar-brand"><%= link_to "Registro de Incorporación", root_path, "aria-label": "Ir a la página de inicio" %></h1>

        <div class="navbar-menu">
          <% if user_signed_in? %>
            <span class="navbar-user">Hola, <%= current_user.email %></span>
            <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, form: { style: "display:inline-block;" }, params: {}, class: "btn btn-danger", "aria-label": "Cerrar sesión de usuario" %>
          <% else %>
            <%= link_to "Crear cuenta", new_user_registration_path, class: "btn btn-primary", "aria-label": "Crear una nueva cuenta" %>
            <%= link_to "Iniciar sesión", new_user_session_path, class: "btn btn-success", "aria-label": "Iniciar sesión en el sistema" %>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="main-layout">
      <!-- Sidebar Menu (visible only when signed in) -->
      <% if user_signed_in? %>
        <aside class="sidebar">
          <nav>
            <div class="sidebar-section">
              <p class="sidebar-title">Menú</p>
              <ul class="sidebar-menu">
                <li>
                  <%= link_to "Inicio", root_path, class: "sidebar-link", "aria-label": "Ir a la página de inicio" %>
                </li>
                <% if current_user.aspirante? %>
                  <li>
                    <% curriculum = current_user.curriculum %>
                    <% if curriculum&.persisted? %>
                      <%= link_to "Mi currículum", curriculum_path(curriculum), class: "sidebar-link", "aria-label": "Ver mi currículum" %>
                    <% else %>
                      <%= link_to "Crear mi currículum", new_curriculum_path, class: "sidebar-link", "aria-label": "Crear mi currículum profesional" %>
                    <% end %>
                  </li>
                <% end %>
              </ul>
            </div>

            <!-- Admin Menu -->
            <% if current_user.admin? %>
              <div class="sidebar-section bordered">
                <p class="sidebar-title">Administración</p>
                <ul class="sidebar-menu">
                  <li>
                    <%= link_to "Gestión de Usuarios", admin_users_path, class: "sidebar-link admin", "aria-label": "Administrar usuarios del sistema" %>
                  </li>
                </ul>
              </div>
            <% end %>

            <!-- Recruiter Menu -->
            <% if current_user.reclutador? %>
              <div class="sidebar-section bordered">
                <p class="sidebar-title">Reclutamiento</p>
                <ul class="sidebar-menu">
                  <li>
                    <%= link_to "Gestión de Aspirantes", recruiter_aspirants_path, class: "sidebar-link recruiter", "aria-label": "Administrar aspirantes registrados" %>
                  </li>
                </ul>
              </div>
            <% end %>
          </nav>
        </aside>
      <% end %>

      <div class="main-content">
        <% if notice %>
          <div class="alert-success">
            <strong><%= notice %></strong>
          </div>
        <% end %>

        <% if alert %>
          <div class="alert-danger">
            <strong><%= alert %></strong>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </div>
  </body>
</html>
