<div class="max-w-4xl mx-auto px-4 md:px-0">
  <div class="card mb-6">
    <div class="flex flex-wrap justify-between items-center gap-4">
      <h2 class="heading-2">📢 Detalle de Convocatoria</h2>
      <div class="flex gap-2">
        <%= link_to "✏️ Editar", edit_recruiter_job_posting_path(@job_posting), class: "btn btn-warning" %>
        <%= link_to "⬅️ Volver", recruiter_job_postings_path, class: "btn btn-secondary" %>
      </div>
    </div>
  </div>

  <div class="bg-white rounded-lg shadow-lg p-6">
    <!-- Imagen -->
    <% if @job_posting.image.attached? %>
      <div class="mb-6 text-center">
        <%= image_tag @job_posting.image, class: "max-w-full h-auto max-h-96 mx-auto rounded-lg shadow" %>
      </div>
    <% end %>

    <!-- Información -->
    <div class="space-y-4">
      <div>
        <h3 class="text-lg font-semibold text-gray-700">💼 Cargo</h3>
        <p class="text-xl text-gray-900"><%= @job_posting.title %></p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <h3 class="text-lg font-semibold text-gray-700">📅 Fecha de Inicio</h3>
          <p class="text-gray-900"><%= l(@job_posting.start_date, format: :long) %></p>
        </div>

        <div>
          <h3 class="text-lg font-semibold text-gray-700">📅 Fecha de Fin</h3>
          <p class="text-gray-900"><%= l(@job_posting.end_date, format: :long) %></p>
        </div>
      </div>

      <div>
        <h3 class="text-lg font-semibold text-gray-700">📊 Estado</h3>
        <% case @job_posting.status %>
        <% when "creado" %>
          <span class="px-3 py-1 inline-flex text-sm font-semibold rounded-full bg-gray-100 text-gray-800">
            📝 Creado
          </span>
        <% when "vigente" %>
          <span class="px-3 py-1 inline-flex text-sm font-semibold rounded-full bg-green-100 text-green-800">
            ✅ Vigente
          </span>
        <% when "cerrado" %>
          <span class="px-3 py-1 inline-flex text-sm font-semibold rounded-full bg-red-100 text-red-800">
            🔒 Cerrado
          </span>
        <% end %>
      </div>

      <!-- Acciones de estado -->
      <div class="pt-4 border-t border-gray-200">
        <h3 class="text-lg font-semibold text-gray-700 mb-3">⚙️ Acciones</h3>
        <div class="flex gap-3">
          <% if @job_posting.creado? %>
            <%= button_to "🚀 Publicar Convocatoria", publish_recruiter_job_posting_path(@job_posting),
                method: :patch,
                class: "btn btn-success",
                form: { data: { turbo_confirm: "¿Publicar esta convocatoria? La fecha de inicio cambiará a hoy." } } %>
          <% end %>

          <% if @job_posting.vigente? %>
            <%= button_to "🔒 Cerrar Convocatoria", close_recruiter_job_posting_path(@job_posting),
                method: :patch,
                class: "btn btn-warning",
                form: { data: { turbo_confirm: "¿Cerrar esta convocatoria?" } } %>
          <% end %>

          <%= button_to "🗑️ Eliminar", recruiter_job_posting_path(@job_posting),
              method: :delete,
              class: "btn btn-danger",
              form: { data: { turbo_confirm: "¿Estás seguro de eliminar esta convocatoria?" } } %>
        </div>
      </div>
    </div>
  </div>
</div>
