<div class="recruiter-container">
  <h2 class="recruiter-header">Panel de Reclutador - Gestión de Aspirantes</h2>

  <div class="recruiter-actions">
    <%= link_to "Crear nuevo aspirante", new_recruiter_aspirant_path, class: "btn btn-primary" %>
  </div>

  <% if @aspirants.any? %>
    <div class="table-container">
      <table class="data-table">
        <thead>
          <tr>
            <th>Email</th>
            <th>ID</th>
            <th>Creado</th>
            <th class="center">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% @aspirants.each do |aspirant| %>
            <tr>
              <td><%= aspirant.email %></td>
              <td><%= aspirant.id %></td>
              <td><%= aspirant.created_at.strftime("%d/%m/%Y %H:%M") %></td>
              <td class="center">
                <%= link_to "Editar", edit_recruiter_aspirant_path(aspirant), class: "btn btn-warning btn-sm" %>
                <%= link_to "Eliminar", recruiter_aspirant_path(aspirant), method: :delete, data: { confirm: "¿Está seguro?" }, class: "btn btn-danger btn-sm" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <p class="empty-state">No hay aspirantes registrados en el sistema.</p>
  <% end %>
</div>
